services:
  meme-web:
    build:
      context: ..
      dockerfile: Dockerfile
    ports:
      - "2233:2233"
    environment:
      - LANG=en_US.UTF-8
      - MALLOC_ARENA_MAX=2
    restart: unless-stopped
