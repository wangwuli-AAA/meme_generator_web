[tool.poetry]
name = "meme-web"
version = "0.1.0"
description = "Unified meme generator web application"
authors = []
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.9"
meme-generator = "0.1.14"
toml = "^0.10.2"
pycairo = ">=1.20.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
